import Module from "../structures/Module";

export default new Module({
    name: "playground",
    event: "ready",
    async run(client) {
        // console.log("playground on messageCreate");

        return;
    },
});
